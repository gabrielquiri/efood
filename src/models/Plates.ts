class Plates {
  id: number
  name: string
  description: string

  constructor(id: number, nome: string, descricao: string){
    this.id = id;
    this.name = nome;
    this.description = descricao;
  }
}

export default Plates